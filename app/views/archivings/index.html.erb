<% if admin_user? %>

	<nav class="admin post_control">
		<%= link_to "New Archiving", new_archiving_path %>
	</nav>

<% end %>

<% if @archivings.empty? %>

	<h2>There isn't anything here...</h2>

<% else %>

	<%= render @archivings %>

<% end %>