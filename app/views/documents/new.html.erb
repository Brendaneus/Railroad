<%= form_for [@archiving, @document] do |f| %>
	<div>
		<%= f.label :name %>
		<%= f.text_field :name %>
	</div>

	<div>
		<%= f.label :file %>
		<%= f.file_field :file %>
	</div>

	<div>
		<%= f.label :content %>
		<%= f.text_area :content %>
	</div>

	<%= f.submit %>
<% end %>